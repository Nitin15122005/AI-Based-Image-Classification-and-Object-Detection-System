# Copy to `.env.local` to point the frontend at the real FastAPI backend
# instead of the built-in mock store. Leave unset (or empty) to keep using
# the local mock — this is the default, and what a Vercel deploy with no
# backend configured yet will fall back to.
VITE_API_BASE_URL=http://localhost:8000
